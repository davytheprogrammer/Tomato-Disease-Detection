# Django Settings
SECRET_KEY=your-secret-key-here-change-this-in-production
debug=True

# Database (default is SQLite for development)
# For PostgreSQL in production, set:
# DATABASE_URL=postgres://username:password@localhost:5432/tomato_ai

# Allowed Hosts (comma-separated for production)
# ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Model Path (optional - defaults to models/best_mobilenet_finetuned.keras)
# MODEL_PATH=/path/to/your/model.keras

# Deployment Settings
DEBUG=False
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=31536000
